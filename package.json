{
  "name": "aiark-top",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npx --yes serve . -l 4173",
    "lint": "npm run check",
    "build": "npm run check",
    "check": "node --check app.js"
  }
}
